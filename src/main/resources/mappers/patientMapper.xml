<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.smartChart.patient.entity.PatientMapper">
    <select id="selectPatientList" resultType="map">
        SELECT
            *
        FROM
            `patient`

    </select>
</mapper>
